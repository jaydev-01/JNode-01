export default class EncryptDecryptFunction {
  private encryptionAlgo = "aes-256-cbc";
  encrypt(dataToEncrypt: any): string {
    return "";
  }

  decrypt(encryptedString: string): any {
    return "";
  }
}
